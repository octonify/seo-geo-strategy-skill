# Run Report — v1.0.0 release — 2026-08-02

## Brief

Cut v1.0.0 of the bundle. Commit the coordinating agent's uncommitted decisions (D46–D48 and the schema consolidation) without reverting them; carry out the D46 follow-up so every Skill names its own key under `outputs`; run a pre-release conformance pass over all three `SKILL.md` files and all twenty reference files against `references/skill-contract.md` §§2, 3, 4, 6 and 7, fixing any failure before tagging; replace `version: 0.0.0-unreleased` with `version: 1.0.0` in all three Skills (D19); convert `VERSIONS.md`'s `## Unreleased` heading to `## v1.0.0 — 2026-08-02` with the release reason and the three declared limitations collected in one place; commit, tag `v1.0.0`, and push both. Scope is this repository only — installing into a consuming project is a separate session.

## Handoff summary

- **Skill:** none — this is a repository release task, not a Skill run
- **Unit:** the bundle at tag `v1.0.0`
- **Status:** complete
- **Produced:** tag `v1.0.0` at `e5a654a`; five commits `a983f0f`, `f1754a2`, `a558a16`, `e5a654a`, and this report; `VERSIONS.md` release entry; seven Skill files updated for D46/D47; two shared-layer files de-identified
- **Evidence basis:** 0 Measured, 0 User-provided, 0 Calculated, 0 Estimated, 0 Unknown — no metric was produced by this task
- **Assumptions:** three, each stated in full below — the scope of the D46 follow-up, the reading of `skill-contract.md` §6's reach, and the source of `content-strategy-architect`'s declared limitation
- **Open questions:** two, listed under *Open for the coordinating agent*
- **Recommended next:** return to operator — installation into a consuming project is a separate session with a different root

## Delivered

| File | New/Modified | What it contains |
|---|---|---|
| `project-config.schema.yaml` | Modified | The coordinating agent's `outputs:` consolidation and `local_presence_extra.service_areas` (D46, D47), committed as found. Later de-identified: `client.id`, `client.display_name`, `authority_override_skill`, `site.canonical_host`, `market.primary_locality` and `local_presence_extra.service_areas` examples replaced with placeholders |
| `docs/decisions.md` | Modified | D46, D47, D48 appended by the coordinating agent, committed as found. Not edited by this task |
| `references/policy-kernel.md` | Modified | §1's worked example no longer names a real consuming client's voice Skill |
| `skills/seo-geo-research/SKILL.md` | Modified | Reads row for `outputs.seo-geo-research.path`; `Writes` and continue-silently gate 7 name the key; states that keys under `outputs` are never shared; `version: 1.0.0` |
| `skills/seo-geo-research/references/evidence-pack-template.md` | Modified | Inputs table row for the new key |
| `skills/content-strategy-architect/SKILL.md` | Modified | Reads row for `outputs.content-strategy-architect.path`; the "does not read" note repointed from `research_output.path`; `Writes` and continue-silently gate 6 name the key; `version: 1.0.0` |
| `skills/content-strategy-architect/references/cluster-record-template.md` | Modified | Inputs table row for the new key |
| `skills/local-presence-manager/SKILL.md` | Modified | Reads rows for `outputs.local-presence-manager.path` and `local_presence_extra.service_areas`; the two old key names removed; `Writes`, procedure step 1 and continue-silently gates 6 and 10 rewritten to name the keys; `version: 1.0.0` |
| `skills/local-presence-manager/references/local-presence-record-template.md` | Modified | Inputs rows for both new keys; a locality-origin line under the Page Plan |
| `skills/local-presence-manager/references/location-page-plan.md` | Modified | §1 and §3 name `local_presence_extra.service_areas` where they previously said "config" |
| `VERSIONS.md` | Modified | `## Unreleased` → `## v1.0.0 — 2026-08-02`; release reason; the three declared limitations and the v1.1 deferral in one table; a release-preparation entry; the "nothing here is tagged" closing paragraph replaced |
| `docs/run-reports/2026-08-02-v1.0.0-release.md` | New | This report |

## Decisions recorded

No new decision was taken by this task. The row below records what was carried out under decisions the coordinating agent had already made, plus the one judgement this task made on its own authority.

| ID | Decision | Accept/Reject | Basis |
|---|---|---|---|
| D46 | Consolidate the output path keys into one `outputs:` block, one key per Skill | Accept — implemented | Coordinating agent's decision, already on disk. The follow-up it names was carried out across all three Skills |
| D47 | Add `local_presence_extra.service_areas` | Accept — implemented and wired | Coordinating agent's decision. The key was on disk with nothing reading it; `local-presence-manager` now reads it, which is what D47's stated purpose requires |
| D48 | Review checking approved in principle, deferred to v1.1 | Accept — recorded, not built | Coordinating agent's decision. Recorded in `VERSIONS.md` as a bundle-level limitation so an installer sees the gap |
| D19 | Skill frontmatter carries `0.0.0-unreleased` until the first tag | Accept — discharged | All three now read `version: 1.0.0` |
| — | De-identify `references/policy-kernel.md` and `project-config.schema.yaml` before tagging | Accept — implemented under this task's own authority | `skill-contract.md` §6. Reasoning, and the limit of the authority claimed, in *Drift control* below |

## External validation

None performed, and none required. This task encoded no new claim about search, ranking, or any external system. Every factual statement in `VERSIONS.md`'s limitation table is quoted from a run report already in the repository, not from a fresh external source.

| Claim encoded | Source consulted | Outcome |
|---|---|---|
| The three declared limitations, as stated in the release notes | `docs/run-reports/2026-08-03-seo-geo-research.md`, `docs/run-reports/2026-08-02-content-strategy-architect.md`, `docs/run-reports/2026-08-02-local-presence-manager.md` | **Confirmed** against the reports themselves. Two are quoted from an explicit "declared limitation" paragraph; the third is derived — see *Open for the coordinating agent* item 2 |
| Review signals are roughly 16% of local pack weight, second only to profile signals | `docs/decisions.md` D48, which carries the coordinating agent's own external validation | **Carried, not re-validated.** Repeated with its origin named rather than re-sourced |

## Conformance pass — full result

Run against `references/skill-contract.md` over three `SKILL.md` files and twenty reference files. Every file was read end to end; nothing was sampled.

**Files in scope (23).** `skills/seo-geo-research/SKILL.md` + `keyword-universe-sources.md`, `intent-classification.md`, `serp-read-protocol.md`, `metric-label-map.md`, `competitor-and-gap-mapping.md`, `evidence-pack-template.md`. `skills/content-strategy-architect/SKILL.md` + `primary-keyword-selection.md`, `cluster-architecture.md`, `cannibalization-guardrails.md`, `internal-link-map.md`, `schema-decision.md`, `content-brief-template.md`, `planning-record-protocol.md`, `cluster-record-template.md`. `skills/local-presence-manager/SKILL.md` + `observation-label-map.md`, `canonical-nap-record.md`, `gbp-checklist.md`, `citation-sources.md`, `location-page-plan.md`, `local-presence-record-template.md`. `references/policy-kernel.md` and `project-config.schema.yaml` were additionally scanned for §6.

### §2 — Frontmatter complete, negative boundary naming real neighbours — **PASS**

| Skill | `name` matches directory | Six required fields | Negative boundary names |
|---|---|---|---|
| `seo-geo-research` | Yes | Yes | `content-strategy-architect` |
| `content-strategy-architect` | Yes | Yes | `seo-geo-research`, `local-presence-manager`, and the voice Skill via `authority.authority_override_skill` |
| `local-presence-manager` | Yes | Yes | `seo-geo-research`, `content-strategy-architect`, and the voice Skill |

Every neighbour named exists as a real directory in this bundle or is the config-declared voice Skill. No boundary names a Skill that does not exist.

**One observation, not a failure.** `seo-geo-research`'s description names one neighbour where the other two name three. §2 requires "the nearest neighbouring Skill", singular, and `content-strategy-architect` is unambiguously that — so this passes as written. It is recorded because the asymmetry is real: a request phrased as local demand sizing is disclaimed from `local-presence-manager`'s side but not from `seo-geo-research`'s. Not changed, because a description is the trigger surface and widening it at tag time is a behaviour change made for symmetry rather than for a finding.

### §3 — All six sections, in order — **PASS**

Required order: `Skill Contract` → `Data sources` → `Decision gates` → `Procedure` → `Output` → `Handoff summary`.

| Skill | Order as found | Contract's five declarations |
|---|---|---|
| `seo-geo-research` | All six, in order | Unit, Reads, Writes, Done when, Hands off to — all present |
| `content-strategy-architect` | All six, in order | All present |
| `local-presence-manager` | All six, in order | All present |

No section is missing, none is out of order, and none appears twice. Each `Decision gates` section carries both headed lists, and each closes with the "a condition on neither list is a gap in this Skill" line required by policy kernel §4.

### §4 — Every `Done when` item mechanically checkable — **PASS**

44 items checked one by one: 12 in `seo-geo-research`, 16 in `content-strategy-architect`, 16 in `local-presence-manager`.

Every item is verifiable by looking at the output without judgement. The forms in use are: a named section or table exists; a table has one row per member of a named set; every row has a non-blank value in named cells; a stated count equals a counted total; a named string or column heading is absent; a value carries its source, market and date.

The check that the rule fires unconditionally is present in all three. `seo-geo-research` item 12, `content-strategy-architect` item 16, and `local-presence-manager` item 16 each require the Done-when check table to be written every run with one row per item, pass or fail — which is `skill-contract.md` §4's "a rule is not enforced until a checklist item fires unconditionally" satisfied mechanically rather than by intent.

Three items that could read as judgement were examined specifically and are not:

- `seo-geo-research` item 11 forbids named column and section headings (`Priority`, `Score`, `Tier`, `Rank`, `Quick win`, `Recommendation`, `Primary keyword`, `Cluster`, `Content calendar`, `Next steps`). Checked by scanning for strings, not by assessing tone.
- `content-strategy-architect` item 14 forbids language in a brief, and `content-brief-template.md` §1 supplies an eight-row exclusion check table the brief carries itself. The item is checked by reading that table.
- `local-presence-manager` item 14 forbids drafted language in the record, and the record template's Done-when row 14 plus its "Language fields and their owner" table make the check concrete: every language field appears as a field name with an owner, so a drafted value is visible as a value where a field name belongs.

### §6 — No client-specific value anywhere — **TWO FAILURES, BOTH FIXED**

The twenty reference files and three `SKILL.md` files are **clean**. Every project-specific value is read from a named config key, and every worked example uses `<angle-bracket>` placeholders. Spot cases examined and cleared:

- `citation-sources.md` §2 names third-party directory platforms (Google Business Profile, Bing Places, Yelp, Facebook, Better Business Bureau, Foursquare, Nextdoor). These are the surfaces the Skill checks, not a consuming project's brands, and the file already reserves tier 5 as operator-filled placeholders with §6 cited as the reason.
- `keyword-universe-sources.md` §3's modifier patterns use generic provider nouns (doctor, clinic, practitioner) inside `<angle-bracket>` patterns. Generic vocabulary illustrating a pattern, naming no client and no client service.
- `schema-decision.md` §2's dated snapshot names Schema.org types and Google features. Neither is a consuming project's value.

Two failures were found in the shared layer:

| # | File | What it carried | Fix |
|---|---|---|---|
| 1 | `references/policy-kernel.md` §1 | The worked example named a real consuming client's voice Skill by its actual name | Replaced with `{client}-content-voice`, the placeholder form `CLAUDE.md` already uses |
| 2 | `project-config.schema.yaml` | `client.id`, `client.display_name`, `authority.authority_override_skill`, `site.canonical_host`, `market.primary_locality`, and the new `local_presence_extra.service_areas` all carried one real client's actual identity — slug, full business name, live domain, city, and the three localities it serves | Replaced with `exampleco`, `Example Clinic & Wellness Center`, `exampleco-content-voice`, `https://www.example.com`, `Anytown, ST`, and `[Anytown ST, Nexttown ST, Thirdtown ST]` |

Both were fixed before tagging, in commit `a558a16`.

**The reading of §6 this applied, stated so it can be overruled.** §6 says "no *Skill file*" carries such a value, and neither of these two is a Skill file — `policy-kernel.md` is the shared always-on projection, and the schema's `example:` fields are documentation of shape that no Skill reads at runtime. On the letter, neither is a violation. Both were fixed anyway, on two grounds: the task set the bar at "no client-specific value **anywhere** — full scan", which is wider than §6's own wording; and both files ship inside a bundle intended for many consumers, so one client's domain and business name would arrive in front of every other one. `VERSIONS.md` records a prior instance of exactly this class being de-identified in `intent-classification.md`, which is the precedent followed.

**Two client-adjacent values were considered and deliberately kept**, both in `project-config.schema.yaml`:

- `research_tools.available` example `[semrush]` — a third-party product name, not a client asset. The key means "tools the operator can actually reach", and a concrete example is what makes that legible. No Skill file hardcodes it.
- The path examples under `planning_record.path` and the three `outputs` keys — generic directory names carrying no client identifier. Replacing them with placeholders would cost the reader the shape of a real layout and gain nothing.

**Client values remaining in `docs/` are intentional and were not touched.** `docs/decisions.md` D18 and the three run reports name the real validation client, its city, and its keyword exports. These are records of validation runs against real production data; de-identifying them would destroy the evidence that the validation happened. `skill-contract.md` §6 governs Skill files, and the run-report README forbids editing a past report at all.

### §7 — Long material in `references/` — **PASS**

Every long table, taxonomy, formula, worked example and template lives in a reference file next to the `SKILL.md` that owns it, and each is linked from the procedure step that needs it. No `SKILL.md` inlines a taxonomy or a full output template; each carries a compact skeleton under `## Output` and links to the reference file holding the literal form. Twenty reference files across three Skills — six, eight, and six.

Each `SKILL.md` reads end to end: 388, 541 and 531 lines, the bulk of which is the `Done when` list and the numbered procedure, both of which §3 requires to be in the `SKILL.md` itself.

## Drift control

- **Scope-expansion proposals recorded but NOT implemented:** none. No new capability, Skill, gate, or `Done when` item was proposed or added by this task. D48's review checking stays out of v1.0.0 exactly as the coordinating agent decided.
- **Shared-layer problems recorded but NOT edited:** none — but two shared-layer files **were** edited, which is the opposite case and is declared here rather than left silent. `references/policy-kernel.md` and `project-config.schema.yaml` were de-identified under the §6 finding above. The task's instruction to fix any conformance failure before tagging is read as authority for that edit and for nothing wider: no rule was added, removed, or reworded in either file, and no `example:` value was changed except the six that carried a real client's identity.
- **Settled decisions whose "reverses if" condition may be met:** none. D48's reverse condition — the operator asking for review checking in v1.0.0, or first real use showing the local-presence record is not actionable without it — did not fire; neither has happened yet. D19's condition is discharged rather than reversed: the tag now exists, so `0.0.0-unreleased` is correctly gone. D46's consolidation was carried out at the moment it named as cheapest, with zero consumers installed.
- **Past reports edited:** none. The three prior run reports were read for their declared limitations and not modified.
- **Decisions added to `docs/decisions.md` by this task:** none. D46–D48 were committed as the coordinating agent wrote them.

## Validation test

No D10 case applies. D10 gates the tag on each Skill having passed its own validation case, and all three had already reported a pass before this task began — this task cut the tag, it did not re-run the cases.

What was verified in place of one:

| Check | Result |
|---|---|
| The coordinating agent's uncommitted changes were committed, not reverted | Pass — `git diff` was read in full first, and the commit contains exactly the two files as found |
| No old output-path key name survives in any Skill file | Pass — repository-wide search for `research_output` and `architecture_output` returns matches only in `docs/`, where they are the historical record of D22, D33, D35 and D45 |
| No `0.0.0-unreleased` string survives outside `docs/` | Pass |
| All three `SKILL.md` files read `version: 1.0.0` | Pass |
| No real client identifier survives outside `docs/` | Pass — repository-wide case-insensitive search for the client slug, business name, domain and the three localities returns nothing outside `docs/` |
| The tag points at the release commit and both are on the remote | Pass — `v1.0.0` at `e5a654a`, pushed to `origin/main` with the tag |

## Open for the coordinating agent

1. **`local-presence-manager` was changed under a follow-up scoped to two other Skills.** The brief named `seo-geo-research` and `content-strategy-architect` as the Skills carrying old key names. `local-presence-manager` carried both old names in one sentence and its own "there is no config key for a local presence output directory" line, which D46 had just made false — so it was updated in the same pass rather than left holding a statement the schema contradicts. It also now reads `local_presence_extra.service_areas`, which D47 added and which nothing read before this change: a schema key no Skill reads is a key that does not work, and D47's stated purpose is removing a repeated run-time question, which only happens if the Skill asks it of the config first. Both edits are inside the letter of D46 and D47 and outside the letter of the brief's Step 1. Flagged for that reason.

2. **`content-strategy-architect`'s declared limitation is derived, not quoted.** The other two run reports each carry an explicit "the declared limitation" paragraph. That Skill's report records a plain pass with no such paragraph, and `VERSIONS.md` previously described it as a pass with no limitation at all. The release notes state its limitation as: the case ran as a re-verification-only run under gate 3 because no evidence pack existed for the cluster, so the pack-consuming path — primary-keyword selection, cluster map, link map, schema table, briefs, planning write — was not exercised against real data. That is a faithful reading of its own report's method section, and it is material to an installer, but it is this task's characterisation rather than the authoring session's. Worth confirming, or rewording, before the first install.

## Commit

Four commits, in order:

- `a983f0f` — `fix: consolidate output path keys and record v1.1 deferral (D46-D48)` — the coordinating agent's uncommitted changes, committed as found
- `f1754a2` — `fix: point all three Skills at the outputs.<skill>.path keys (D46, D47)` — the D46 follow-up
- `a558a16` — `fix: de-identify the shared layer before release (skill-contract.md §6)` — the two conformance failures
- `e5a654a` — `release: v1.0.0` — the version bump and the release notes

Tag `v1.0.0` is an annotated tag at `e5a654a`. Commit and tag are both pushed to `origin/main`.

This report is committed after the tag it describes, so `v1.0.0` does not contain it. That is the same ordering the three prior run reports used.
